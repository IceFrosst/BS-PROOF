# Report runs index

Every demo / selftest / wiring audit kept under `reports/runs/`.

| When (UTC) | Mode | Ingredient | Form | Selftest | File |
|---|---|---|---|---|---|
| — | — | — | — | — | _No runs committed yet. Run `python3 scripts/write_demo_report.py --wiring` then commit `reports/`. _ |
