# Archived runs — scoring model `v1-transfer-in-weight`

signed -100..100 only. form x dose x population multiplied into w_study. No arcs. Superseded 2026-08-07.

**Do not compare these numbers with runs under a different model.** The
same corpus produces different scores under each, because the formula
changed — not because the evidence did.

Files here are never EDITED. They record what the pipeline said on the day
they were produced, and rewriting one would make it a different claim.

Pruned 2026-08-09 (founder): the four `v1` run files were deleted outright.
Deleting is not editing — nothing here now misrepresents a past run — and
they remain in git history. What is left is a build report, kept because it
records measurements that no re-run can reproduce.
