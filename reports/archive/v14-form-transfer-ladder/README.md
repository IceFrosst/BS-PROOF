# Archived runs — scoring model `v14-form-transfer-ladder`

the composite's FORM term reads near-form evidence at the transfer factor FORM_FACTOR already defines, instead of exact-form-or-nothing. A non-exact study enters the ladder at FORM_LADDER[rank] x FORM_FACTOR[form_match] for `unspecified` (0.30) and `salt_family` (0.50); `different` (0.15) is still excluded because a form we know is not yours answers a different question. The term is the best of the exact and transfer ladders. Founder ask 2026-08-28, 'less strict but absolutely true'; motivated by omega-3 run 20260828_083440 where six non-negative unspecified RCTs (four at s=+1.00) scored zero form credit while one exact-form trial at s=-0.02 set all_negative_in_form and zeroed the term. NO NEW CONSTANT -- FORM_FACTOR exists to price partial form evidence and was simply not read here. SIGNED SCORE UNCHANGED (invariant 8 keeps form out of w_study); only the composite moves. Arc verdict/coverage/n_in_form stay exact-form only, so invariant 8's 'nobody tested your form' vs 'your form failed' distinction is intact.

**Do not compare these numbers with runs under a different model.** The
same corpus produces different scores under each, because the formula
changed — not because the evidence did.

Files here are never edited or deleted. They record what the pipeline
said on the day they were produced.
